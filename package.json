{
  "name": "paxebonum",
  "version": "0.0.1",
  "description": "Pax Bonum - Répertoire de chants liturgiques",
  "author": "cedrou",
  "private": true,
  "dependencies": {
    "parse": "^2.7.0",
    "react": "^16.9.0",
    "react-bootstrap": "^1.0.0-beta.12",
    "react-dom": "^16.9.0",
    "react-markdown": "^4.2.2",
    "react-router-bootstrap": "^0.25.0",
    "react-router-dom": "^5.0.1"
  },
  "devDependencies": {
    "concurrently": "^4.1.2",
    "nodemon": "^1.19.2",
    "react-scripts": "3.1.1"
  },
  "scripts": {
    "start": "concurrently -k \"nodemon ./backend/bin/www\" \"react-scripts start\"",
    "build": "react-scripts build && mv build backend",
    "test": "react-scripts test --env=jsdom"
  },
  "proxy": "http://localhost:3001",
  "nodemonConfig": {
    "watch": [
      "backend/"
    ]
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
